package db

//"context"

//"fmt"
//"log"
//"time"
//_ "github.com/lib/pq"

// DBService is responsible for managing database connections.
//type DBService struct {
//	db *sql.DB
//}

func DataBuilder(chatType string) map[string]interface{} {

	// Based on chatType, use models package and db to build the data in the required format (for use in template)
	// For now, just return the an empty interface
	return nil

}
