# TODO: Add function call logic
def goal_reached(message):
    """
    Determines if the chat goal is reached using AI techniques.

    Args:
        message (str): The latest user message.

    Returns:
        bool: True if the goal is reached, otherwise False.
    """
    return "Goodbye" in message
